// Optional. Only needed when this page is hosted separately from the assistant (for example on a static
// host). Point it at the assistant's public HTTPS URL. Leave it commented out when the assistant serves
// the page itself: the page then uses its own origin.
// window.SCRIPTSYNC_API = "https://your-assistant.example.com";
